src/components/Settings.tsx:      const users = snap.docs.map(d => d.data() as UserProfile).filter(u => u.uid !== auth.currentUser?.uid);
src/components/Settings.tsx:        setHistory(snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as HistoryItem)));
src/components/Settings.tsx:        setNotifications(snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as AppNotification)));
src/components/Settings.tsx:        await Promise.all(listResult.items.map(item => deleteObject(item)));
src/components/Settings.tsx:        await Promise.all(momentsList.items.map(item => deleteObject(item)));
src/components/Settings.tsx:        setNotifications(prev => prev.map(n => n.id === notif.id ? { ...n, read: true } : n));
src/components/Settings.tsx:      setSearchResults(snap.docs.map(d => d.data() as UserProfile).filter(u => u.uid !== auth.currentUser?.uid));
src/components/Settings.tsx:          {sections.map((section) => (
src/components/Settings.tsx:                {notifications.map((notif) => (
src/components/Settings.tsx:                {history.map((item) => (
src/components/Settings.tsx:                      {[1,2,3,4,5,6].map(n => <option key={n} value={n}>{n} {n === 1 ? 'Child' : 'Children'}</option>)}
src/components/Settings.tsx:                      {(searchQuery ? searchResults : allUsers).map(u => (
src/components/Settings.tsx:                      {['mother', 'father', 'both'].map((type) => (
src/components/Settings.tsx:                      {(searchQuery ? searchResults : allUsers).map(u => (
src/components/Achievements.tsx:        const users = snap.docs.map(d => d.data() as UserProfile);
src/components/Achievements.tsx:          {categories.map((cat) => (
src/components/Achievements.tsx:              {topUsers.map((user, index) => (
src/components/RandomProfiles.tsx:        .map(doc => doc.data() as UserProfile)
src/components/RandomProfiles.tsx:            {users.map((user) => (
src/components/StoryOverlay.tsx:              {sections.map((section, index) => (
src/components/ChatInbox.tsx:      const convs = await Promise.all(snap.docs.map(async (d) => {
src/components/ChatInbox.tsx:    const newConvs = await Promise.all(snap.docs.map(async (d) => {
src/components/ChatInbox.tsx:      const msgs = snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as Message));
src/components/ChatInbox.tsx:            conversations.map((conv) => (
src/components/ChatInbox.tsx:              {messages.map((msg, idx) => {
src/components/MomentUpload.tsx:                  {[12, 24].map((hours) => (
src/components/DiscoverPage.tsx:        .map(doc => doc.data() as UserProfile)
src/components/DiscoverPage.tsx:      const withDistance = allUsers.map(u => ({
src/components/DiscoverPage.tsx:            {nearbyUsers.map((user) => (
src/components/FriendList.tsx:        const results = await Promise.all(snap.docs.map(async (d) => {
src/components/FriendList.tsx:    const results = await Promise.all(snap.docs.map(async (d) => {
src/components/FriendList.tsx:            {filteredFriends.map((friend) => (
src/components/BusinessFeatures.tsx:            {items.map((item, index) => (
src/components/BusinessFeatures.tsx:      setBids(snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as Bid)));
src/components/BusinessFeatures.tsx:            {bids.map((bid) => (
src/components/BusinessFeatures.tsx:      const lists = snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as ShoppingList));
src/components/BusinessFeatures.tsx:      const queries = snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as StockQuery));
src/components/BusinessFeatures.tsx:      const deliveries = snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as DeliveryRequest));
src/components/BusinessFeatures.tsx:            nearbyLists.map(list => (
src/components/BusinessFeatures.tsx:                  {list.items.map((item, i) => (
src/components/BusinessFeatures.tsx:            nearbyQueries.map(query => (
src/components/BusinessFeatures.tsx:            nearbyDeliveries.map(delivery => (
src/components/Moments.tsx:      const newMoments = snap.docs.map(d => ({ id: d.id, ...d.data() as any } as Moment));
src/components/Moments.tsx:      {moments.map((moment, index) => (
src/components/MoodPicker.tsx:  const filteredCategories = MOOD_CATEGORIES.map(cat => ({
src/components/MoodPicker.tsx:                {filteredCategories.map((category) => (
src/components/MoodPicker.tsx:                      {category.emojis.map((mood) => (
src/components/FriendBar.tsx:      const results = await Promise.all(snap.docs.map(async (d) => {
src/components/FriendBar.tsx:          {friends.map((friend) => (
src/components/SplashScreen.tsx:          {[0, 1, 2].map((i) => (
src/components/Sidebar.tsx:        {menuItems.map((item) => (
src/components/RightSidebar.tsx:          .map(doc => doc.data() as UserProfile)
src/components/RightSidebar.tsx:        {nearbyUsers.map((user) => (
src/components/ProfilePage.tsx:                  {[1, 2, 3].map(s => (
src/components/ProfilePage.tsx:                            {filteredCategories.map(c => (
src/components/Navbar.tsx:      setSearchResults(snap.docs.map(doc => doc.data() as UserProfile));
src/components/MapVibe.tsx:      providerInfo: auth.currentUser?.providerData.map(provider => ({
src/components/MapVibe.tsx:        return data.routes[0].geometry.coordinates.map((coord: [number, number]) => ({
src/components/MapVibe.tsx:                  {filtered.map(user => (
src/components/MapVibe.tsx:        .map(doc => doc.data() as UserProfile)
src/components/MapVibe.tsx:      const uids = new Set(snap.docs.map(doc => doc.data().uid));
src/components/MapVibe.tsx:      const signals = snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as HelpSignal));
src/components/MapVibe.tsx:        setFriends(snap.docs.map(doc => doc.id));
src/components/MapVibe.tsx:      const docs = snap.docs.map(doc => ({ id: doc.id, ...doc.data() } as Destiny));
src/components/MapVibe.tsx:        await Promise.all(notifyUids.map(uid => 
src/components/MapVibe.tsx:          await Promise.all(nearbyUsers.map(u => 
src/components/MapVibe.tsx:      const deletePromises = snapshot.docs.map(doc => deleteDoc(doc.ref));
src/components/MapVibe.tsx:  const uniqueVibes = Array.from(new Set(users.map(u => u.mood?.text).filter(Boolean))) as string[];
src/components/MapVibe.tsx:              {users.filter(u => userProfile.parentMode?.children?.includes(u.uid)).map(child => {
src/components/MapVibe.tsx:                      {['day', 'night'].map(t => (
src/components/MapVibe.tsx:                      {['boy', 'girl'].map(g => (
src/components/MapVibe.tsx:                    {['rush', 'quiet'].map(p => (
src/components/MapVibe.tsx:                      {userSearchResults.map((user) => (
src/components/MapVibe.tsx:                ].map((option) => (
src/components/MapVibe.tsx:                    {users.filter(u => friends.includes(u.uid)).map(user => (
src/components/MapVibe.tsx:              {users.filter(u => u.isOnline).length > 0 ? users.filter(u => u.isOnline).slice(0, 20).map((user) => (
src/components/MapVibe.tsx:              {users.filter(u => friends.includes(u.uid)).length > 0 ? users.filter(u => friends.includes(u.uid)).map((user) => (
src/components/MapVibe.tsx:          {userProfile?.parentMode?.enabled && Object.values(parentPaths).map(path => (
src/components/MapVibe.tsx:                    .map(p => [p.lat, p.lng] as [number, number])}
src/components/MapVibe.tsx:              {path.stops.map((stop, idx) => stop && !isNaN(stop.lat) && !isNaN(stop.lng) ? (
src/components/MapVibe.tsx:          {userProfile?.parentMode?.enabled && Object.values(childDestinies).filter(canSeeDestiny).map(dest => {
src/components/MapVibe.tsx:                        .map(p => [p.lat, p.lng] as [number, number])
src/components/MapVibe.tsx:                      .map(p => [p.lat, p.lng] as [number, number])
src/components/MapVibe.tsx:        }).map((destiny, dIdx) => {
src/components/MapVibe.tsx:                        .map(p => [p.lat, p.lng] as [number, number])
src/components/MapVibe.tsx:                        .map(p => [p.lat, p.lng] as [number, number])
src/components/MapVibe.tsx:                        .map(p => [p.lat, p.lng] as [number, number])
src/components/MapVibe.tsx:        {filteredUsers.map((user) => (
src/components/MapVibe.tsx:        {helpSignals.map((signal, sIdx) => {
src/components/MapVibe.tsx:                          .map(user => (
src/components/ActiveUsers.tsx:        .map(doc => doc.data() as UserProfile)
src/components/ActiveUsers.tsx:        {users.map((user) => (
src/components/SearchPage.tsx:        setSearchResults(snap.docs.map(doc => doc.data() as UserProfile));
src/components/SearchPage.tsx:            {searchResults.map((user) => (
src/App.tsx:        ].map((item, idx) => (
